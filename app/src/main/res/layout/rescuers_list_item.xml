<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginVertical="1dp"
    android:id="@+id/rescuers_list_view_item">

    <android.support.v4.widget.CircleImageView
        android:id="@+id/availabilty_image"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:tint="@color/notAvailable"
        android:src="@drawable/ic_avalibility_0opacity_24dp"
        android:backgroundTint="@color/notAvailable"/>

    <TextView
        android:id="@+id/rescuer_name"
        style="@style/TextAppearance.AppCompat.Large"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="16dp"
        app:layout_anchor="@id/rescuers_list_view_item"
        app:layout_anchorGravity="left|center_vertical"/>

    <TextView
        android:id="@+id/rescuer_distance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="16dp"
        app:layout_anchor="@id/rescuers_list_view_item"
        app:layout_anchorGravity="right|center_vertical"/>

</android.support.design.widget.CoordinatorLayout>